# Todoist Tool — Issues

Outstanding issues from code reviews. Critical and high severity issues were resolved in the 2026-03-30 review cycle.

## Resolved (2026-04-02)

| ID   | Severity | File              | Issue                                                                                                                      | Resolution                                                                                                                                                     |
| ---- | -------- | ----------------- | -------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A-01 | Medium   | `export.py`       | claude config block defined but never rendered — `config.claude.label`, `config.claude.role.repo`, and `config.claude.role.branch` were parsed and stored but `export.py` never included them in task output, making the Claude dispatch metadata invisible. | `_render_task_block()` now emits a `claude:` block when a task has the Claude label applied. `repo:` and `branch:` lines included when configured.               |
| A-02 | Low      | `create.py`, `push.py` | Priority values not validated — Todoist API expects 1–4 but no validation existed locally; out-of-range values were passed directly to the API, which would reject or silently default them. | Added `_validate_priority()` in `create.py` (clamping out-of-range values to 1 with a warning). Imported and applied in `push.py`. `%d` format specifier changed to `%s` to prevent `TypeError` on unexpected `None` input. |

## Resolved (2026-03-30)

| ID   | Severity | File                              | Issue                                                                                                                   | Resolution                                                                                                                                                  |
| ---- | -------- | --------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R-01 | High     | `db/queries.py`                   | `INSERT OR REPLACE` destroyed `created_at` on every re-pull for all five upsert functions                               | Replaced with `INSERT ... ON CONFLICT(id) DO UPDATE SET`, explicitly excluding `created_at` from updates                                                    |
| R-02 | High     | `db/schema.py`                    | `get_data_dir()` returned bare env-var path without appending `todoist/` subdirectory                                   | Added `/ "todoist"` after the env override path so it matches the convention of every other tool                                                            |
| R-03 | Low      | `tests/todoist/test_cli.py`       | `test_all_subcommands_are_registered` did not include `create` or `complete` subcommands                                 | Added `test_create_and_complete_subcommands_are_registered` and updated the expected list                                                                   |
| R-04 | Low      | `tests/todoist/test_schema.py`    | Two `TestGetDataDir` tests were asserting the old (broken) env-override behavior                                        | Updated tests to assert that the env override path has `todoist/` appended                                                                                  |
| M-01 | Medium   | `export.py`                       | `export_to_markdown` silently exported nothing when `config.projects` was empty                                         | Added branch: empty `config.projects` now exports all projects in the DB, mirroring `pull`'s `_filter_api_projects_to_configured` logic                     |
| M-02 | Medium   | `export.py`                       | `_render_task_block` used `or`-based priority fallback (`task.get("priority") or 1`)                                    | Replaced with explicit `is not None` check: `raw_priority if raw_priority is not None else 1`                                                               |
| M-03 | Medium   | `push.py`, `db/queries.py`        | `push.py` had raw SQL outside the `queries.py` data-access layer                                                        | Extracted query into `get_project_ids_by_name(conn, name)` in `queries.py`; `push.py` now calls that function                                              |
| M-04 | Medium   | `export.py`                       | `_get_poster_name` had a permanently dead `conn: sqlite3.Connection` parameter                                           | Removed the unused `conn` parameter and updated the call site in `_render_comment_line`                                                                     |
| M-05 | Medium   | `db/schema.py`                    | Migration SQL parsing was fragile (comment stripping + semicolon split)                                                  | Replaced with `conn.executescript(migration_sql)`; removed `conn.execute("BEGIN;")` and manual split                                                        |
| M-06 | Medium   | `cli.py`                          | `validate_config` was only called in `cmd_config`, not in operation commands                                             | Added `_warn_on_config_issues()` helper; called it in `cmd_pull`, `cmd_push`, `cmd_sync`, and `cmd_export` immediately after `_load_config_from_args`       |
| L-01 | Low      | `cli.py`                          | `cmd_diff` used `or`-based priority fallback (`task_row.get("priority") or 1`)                                           | Replaced with explicit `is not None` check: `raw_priority if raw_priority is not None else 1`                                                               |
| L-02 | Low      | `tests/todoist/test_cli.py`       | No handler-level test coverage for `cmd_create` or `cmd_complete`                                                        | Added `TestCmdCreate` (success, dry-run, missing-project-flag) and `TestCmdComplete` (success, dry-run, unknown-task-error) test classes                    |
| L-03 | Low      | `pull.py`                         | Snapshot files accumulated without any pruning                                                                           | Added `_prune_old_snapshots()` helper and `keep_snapshots` param to `pull()` and `sync()`; `--keep-snapshots N` flag added to `pull` and `sync` subcommands |
| L-04 | Low      | `config.py`                       | `ClaudeConfig.label` and `.repo` defaulted to empty strings instead of `None`                                            | Changed types to `str \| None`, defaults to `None`; updated `_parse_claude_config` to set `None` when values are absent or empty                            |
